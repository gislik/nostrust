// use crate::event::Event;
// use crate::request::Request;

// Messages are sent from clients to relays
// pub enum Message {
//     Event(Event),
//     Request(Request),
//     Close(String),
// }
